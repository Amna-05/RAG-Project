import { LoginForm } from "@/components/auth/LoginForm";

/**
 * Login page
 * 
 * Simple page that renders LoginForm
 * Layout handled by (auth)/layout.tsx
 */
export default function LoginPage() {
  return <LoginForm />;
}