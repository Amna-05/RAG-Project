# Railway.app deployment configuration
# RAG Backend + PostgreSQL (no Redis needed - uses in-memory rate limiting)

[build]
builder = "dockerfile"
dockerfile = "Dockerfile"

# Run database migrations before starting the app
[[deploy.pre-start]]
command = "alembic upgrade head"
